<%= simple_form_for @book do |f| %>
  
  <%= f.select :category_id, @categories %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :author %>
  <%= f.button :submit %>

<% end %>

<%= link_to "Back", root_path %>
